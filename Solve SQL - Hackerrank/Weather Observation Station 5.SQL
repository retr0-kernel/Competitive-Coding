/*
Enter your query here.
*/
SELECT CITY, LENGTH(CITY) FROM (SELECT CITY FROM STATION ORDER BY LENGTH(CITY), CITY) AS A LIMIT 1;

SELECT CITY, LENGTH(CITY) FROM (SELECT CITY FROM STATION ORDER BY LENGTH(CITY)DESC,  CITY) AS B LIMIT 1;
